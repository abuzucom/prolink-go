# Template drift

This file records local differences against the `abuzucom/agents` policy
template. [`DRIFT.md`](https://github.com/abuzucom/agents/blob/main/DRIFT.md)
in the template repository holds the drift policy and assigns this file to the
adopting repository.

Adopted from template commit `d5e040e7736fbdcd67f4d3d43db0650789630452`.

## Adoption scope

`prolink-go` took the full template: the policy family, every portable checker,
the complete Claude Code hook matrix, the client configuration, the test suite,
hook coverage tracking, the pre-commit registration, and CI. Rule 13 required no
pruning of enforcement claims. Every hook and checker that `AGENTS.md` names is
present.

### Taken

- `AGENTS.md` plus the eight synchronized copies generated by
  `scripts/sync.py`.
- `scripts/`: all 25 files, including `check_compliance_tree.py`. That script
  is a plain checker and needs no workflow to be useful.
- `hooks/`: every file, including both parsers, the shared gate core, the
  platform policy, all three shell gates, the infrastructure gate, the branch
  and identity gates, the consent gate, and the policy reinjection hook.
- `.claude/settings.json`, `.codex/config.toml`, `.codex/hooks.json`,
  `.gemini/settings.json`, `.agents/hooks.json`.
- `tests/`: the full suite.
- `tools/hook-trace/sitecustomize.py`, `hook-coverage-baseline.json`,
  `shared-files.json`.
- `.gitattributes`, `.editorconfig`, `.claudeignore`,
  `.pre-commit-config.yaml`, `requirements-checkers.txt`.
- `.github/workflows/sync-check.yml`, `.github/workflows/agents-compliance.yml`,
  and `.github/workflows/agents-md-compliance.yml`.
- `.github/PULL_REQUEST_TEMPLATE.md`, `.github/ISSUE_TEMPLATE.md`.
- `SECURITY.md`, `CONTRIBUTING.md`, `plan/HANDOFF.md`, filled from the
  matching `.example` files. `plan/HANDOFF.md.example` stays in the tree
  alongside the filled copy.

### Declined

- `.github/workflows/immutable-conflict-check.yml`. The workflow runs on the
  privileged `pull_request_target` trigger. Adopting a privileged trigger is a
  security decision this repository did not take.
  `scripts/check_compliance_tree.py` ships anyway. Twelve of the scanner
  tests covering it still run.
- `DRIFT.md` and `adopters/`. Both belong to the template repository.
- The template `README.md`, `CHANGELOG.md`, `Makefile`, `LICENSE`, and
  `docs/gate-threat-model.md` as verbatim copies.

## Local differences

### Expected to differ

- `.claudeignore`. This repository drops the JavaScript-oriented entries
  `node_modules/`, `dist/`, and `.next/`. It adds `vendor/` and `coverage.out`.
  The repository is a Go module.
- `.editorconfig`. A `[*.go]` section sets `indent_style = tab`. Gofmt uses
  tabs. The template default sets four spaces.
- `.gitignore`. This repository adds one, covering Python byte-compiled output
  from the policy tooling plus Go build and coverage artifacts.
- `Makefile`. `PROSE_FILES` names this repository's prose. A separate
  `HEDGING_FILES` list omits `README.md`. `README.md` carries inherited MIT
  prose in the original author's voice. Rewriting it falls outside adoption
  scope under Rule 4. `make check` also runs `sync.py --check-shared`.
- `.github/workflows/sync-check.yml`. Same job layout as the template:
  `tests-windows`, `tests-macos`, and `check-sync`. The prose file lists name
  this repository's prose. This repository drops the template's AgentLint
  step. No new third-party action dependency enters under Rule 9.
- `.github/workflows/main.yml`. The inherited Go build workflow, with two
  changes recorded under the license boundary below.
- `hook-coverage-baseline.json`. Copied from the template verbatim, not
  regenerated here. `check_hook_coverage.py --write-baseline` refuses to run as
  root, because a mode 000 file stays readable for root and the
  permission-dependent branches would record as unreached. The adopting
  environment ran as root. Every file under `hooks/` is byte-identical to the
  template. The template's CI-measured baseline therefore describes this
  checkout exactly. `python scripts/check_hook_coverage.py`
  exits 0 against it. Regenerate the baseline as an ordinary user after any
  future change to `hooks/`.
- `shared-files.json`. Regenerated locally with
  `python scripts/sync.py --write-shared`. The digests match the template files
  as copied.

### Declined wiring assertions

Two test files carried assertions that hold only inside `abuzucom/agents`. An
active human signed off on each edit under Rule 3. Every other assertion in
both files stays byte-identical. No assertion became softer. Neither edit
skipped or widened an assertion.

`tests/test_check_conflict_markers.py`, class `WiringTest`:

- Removed `test_immutable_workflow_uses_only_the_base_checker`,
  `test_privileged_workflow_has_one_immutable_job`,
  `test_privileged_workflow_does_not_execute_pr_authored_tools`,
  `test_privileged_trigger_has_read_only_permissions_and_no_secrets`, and
  `test_security_jobs_use_trusted_base_checkers`. All five read the declined
  `immutable-conflict-check.yml`.
- Removed the `IMMUTABLE_WORKFLOW_PATH` half of
  `test_untrusted_checks_use_the_standard_pull_request_event` and
  `test_pr_draft_semantics_remain_explicit`. Each keeps every `sync-check.yml`
  assertion. The first also keeps its `agents-md-compliance.yml` assertion.
- Dropped `README_PATH` from `test_handoff_requires_active_user_request` and
  `test_handoff_prescribes_no_pre_consent_git_command`. Both still assert
  against `AGENTS.md` and `plan/HANDOFF.md.example`. Both carry every asserted
  policy string. This repository's `README.md` documents a Go library rather
  than the template's policy.

`tests/test_immutable_compliance.py`:

- Removed `test_current_privileged_workflow_policy_passes`,
  `test_privileged_checkout_cannot_redirect_trusted_base`, and
  `test_privileged_scan_cannot_continue_on_error` from
  `ImmutableComplianceScannerTest`. All three read the declined workflow.
- Replaced `ImmutableWorkflowTest` with `WorkflowPinningTest`, preserving
  `test_external_actions_are_pinned_to_full_commit_shas`. That check reads only
  the workflow directory through a glob. It keeps covering every workflow
  this repository ships. The rest of the class read the declined workflow.
- Removed the now-unused `WORKFLOW_PATH` constant.

The preserved pin check reported a finding immediately. The inherited
`main.yml` referenced three actions by tag. Rule 9 forbids that form.

### Environment limitations

- Rule 16 routes hosted GitHub operations through
  `python scripts/trusted_gh.py run <gh arguments>`. The Claude Code web
  environment provides no `gh` executable. That routing stays unavailable
  there. The wrapper ships for local operator use. The instruction remains
  binding without mechanical coverage in that environment.
- The library needs a live PRO DJ LINK network with real CDJs. CI cannot
  exercise the protocol paths in `status.go`, `remotedb.go`, or `structs.go`.

## License boundary

The repository carries a split license. An active human approved both terms.

Files inherited from the initial fork of `evanpurkhiser/prolink-go` stay MIT,
copyright Evan Purkhiser, under `LICENSE`:

- `prolink.go`, `device.go`, `remotedb.go`, `status.go`, `structs.go`
- `bpm/bpm.go`, `mixstatus/handler.go`, `cmd/status-reciever/main.go`
- `README.md`, `go.mod`, `go.sum`, `.golangci.yml`, `LICENSE`
- `.github/workflows/main.yml`

Every path outside that list carries BSD-3-Clause, copyright ABUZUCOM LLC,
under `LICENSE.BSD-3-Clause`. The copied `abuzucom/agents` material retains its
applicable notices and conditions through that file.

Three edits touch inherited MIT files. The first sets
`persist-credentials: false` in `.github/workflows/main.yml`. The second pins
that workflow's three actions to full commit SHAs, resolved through
`git ls-remote` against each public repository:

- `actions/checkout` to `d23441a48e516b6c34aea4fa41551a30e30af803`, release
  v6.1.0, previously the `v6` tag.
- `actions/setup-go` to `be3c94b385c4f180051c996d336f57a34c397495`, release
  v3.6.1, previously the `v3` tag.
- `golangci/golangci-lint-action` to
  `3a919529898de77ec3da873e3063ca4b10e7f5cc`, release v3.7.0, previously the
  `v3` tag.

Each pin names the commit the tag already pointed at. The workflow keeps its
behavior. The third edit adds the License section to `README.md`. None of the
three moves its file to BSD-3-Clause. This adoption adds no SPDX header to an
inherited source.

## Pending template record

`DRIFT.md` requires an `adopters/prolink-go.md` record inside
`abuzucom/agents`. That record lists the taken and declined files above. The
record ships as a separate change against that repository.
